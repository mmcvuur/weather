/**
 * Environment Configuration Template
 * 
 * Copy this file to 'config.js' and set your own API keys.
 * 'config.js' is git-ignored to prevent leaking credentials.
 */
window.ENV = {
  // CARTO Basemap API Key (optional): If left empty, the app falls back to free Esri Dark Canvas basemap
  CARTO_API_KEY: ""
};
